Built this library in Java 1.7, maven for building the app and spring boot framework.

******** command to install dependencies *********

mvn clean install

********** to run test ************

mvn test